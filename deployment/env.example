# Fusca Pro Lookup Environment Variables
# Copy this file to /etc/fusca-env.conf on your EC2 instance
# Update with your actual credentials

# Database Configuration
DB_HOST=your-database-host.com
DB_PORT=3306
DB_USER=your-db-username
DB_PASSWORD=your-db-password
DB_NAME=your-database-name

# SSH Tunnel Configuration (if needed)
SSH_HOST=your-ssh-tunnel-host.com
SSH_PORT=22
SSH_USER=your-ssh-username
SSH_KEY_PATH=/home/ubuntu/.ssh/tunnel_key

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=change-this-to-a-random-secret-key

